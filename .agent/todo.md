# Sort Orchestrator Tasks

- [x] Create `scripts/sort_orchestrator.sh` with argument parsing (sort type + number list)
- [x] Implement validation for sort type (merge|quick and aliases)
- [x] Implement conditional logic to delegate to `quicksort.sh` or `merge_sort.sh`
- [x] Capture output from the called sort script
- [x] Output sorted list and sort method name
- [x] Add `--help` flag with usage information
- [x] Make script executable with `chmod +x`
